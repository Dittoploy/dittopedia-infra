---
# 🚀 jenkins_master_ip est mis à jour dynamiquement par bootstrap.sh (line ~74)
# Ne pas commiter d'IP réelle ici, utiliser un placeholder
jenkins_master_ip: "10.0.1.176"
agent_secret: "changeme"
agent_name: "worker1"
